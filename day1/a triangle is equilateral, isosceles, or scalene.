Write a program to check whether a triangle is equilateral, isosceles, or scalene.
#include<stdio.h>
void main()
{
	int lin1,lin2,lin3 ;
	printf("Enter the length of sides of the triangle: ");
	scanf("%d%d%d",&lin1,&lin2,&lin3);
	if (lin1 ==lin2 && lin2 == lin3)
	{
		printf("the triangle is equlatrial.");
	}
	else
	{
		if (lin1 == lin2 || lin2 == lin3)
		{
			printf("the triangle is isoscles.");
		}
		else
		{
			printf("the triangle is scalene.");
		}
		
	}
}
