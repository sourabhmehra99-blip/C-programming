// to calclute area of circle
#include<stdio.h>
#define PI  3.14
void main()
{
	float r,area;
	printf("Enter the radius of circle: ");
	scanf("%f",&r);
	//area = 3.14*(r*r);
	area = PI*r*r;
	printf("area of circle is %.3f",area);
}
