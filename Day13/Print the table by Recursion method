// to print a table of number using Recursion without return value
#include<stdio.h>
void table(int n,int i)
{
	if(i<=10)
	{
		printf("%d*%d = %d\n",n,i,n*i);
		table(n,i+1);
	}
}
void main()
{
	int n;
	printf("Enter the number for its table : ");
	scanf("%d",&n);
	table(n,1);
}
