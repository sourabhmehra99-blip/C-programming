// write a sum of a dight of a given number;
#include<stdio.h>
int add(int n)
{

	if(n==0)
	 return 0;
	else
	 return (n%10)+ add(n/10);
}
void main()
{
	int n, sum;
	printf("Enter the number : ");
	scanf("%d",&n);
	printf("SUM : %d", add(n));
}
