// addition of a string or conactrntion
#include<stdio.h>
int main()
{
	char str1[100],str2[100];
	printf("Enter the string1: ");
	gets(str1);
	printf("the first string: ");
	puts(str1);
	
	printf("Enter the string2: ");
	gets(str2);
	printf("the first string2: ");
	puts(str2);
	int len1,len2;
	
	while (str1[len1] !='\0')
	{
		len1++;
	}
	// conactente the string
	while (str2[len2]!='\0')
	{
		str1[len1]=str2[len2];
		  len1++;
		  len2++;	}
		   
		   
	str1[len1]='\0';
	printf("new conactation string %s",str1);

}
