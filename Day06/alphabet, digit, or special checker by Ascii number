Write a program to check whether a given character is an alphabet, digit, or special
//character.
#include<stdio.h>
void main()
{
	char ch;
	printf("Enter the value : ");
	scanf("%c",&ch);
	if (ch >=48 && ch<=57)
	{
		printf("the value is dight. ");
		
	}
	else
	{
		if (ch >=32&& ch <=47 || ch >=58 && ch<=64|| ch>=91 && ch<=96 || ch>=123 &&ch<=127)
		{
			printf("the value is special character.");
		}
		else
		{
			if (ch >=65&& ch<=90 || ch >=97&& ch <=122)
			{
				printf("the value is letter");
			}
		}
	}
	
}
