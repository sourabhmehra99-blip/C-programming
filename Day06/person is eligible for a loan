Write a program to determine if a person is eligible for a loan
#include<stdio.h>
void main()
{
	int age,cash,cs;
	printf("Enter the age ,income,credit Score: ");
	scanf("%d%d%d",&age,&cash,&cs);
	if (age >=18 && age <=60){
		if (cash>=15000){
			if (cs>=700){
				printf("you are eligible for loan");
			} else{
				printf("you are not eligible for loan");
			}
		} else { 
		    	printf("you are not eligible for loan");}
		 } else {
	         printf("you are not eligible for loan");
		}
		
}
