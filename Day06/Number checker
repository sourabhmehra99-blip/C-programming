//question-1 write a program to check a number is positive ,negative or zero 
#include <stdio.h>
void main()
{
	int num;
	printf("Enter the number: ");
	scanf("%d",&num);
	if (num>=0)
	{
		if (num>0)
		{
			printf("number is positive.");
		}
		else
		{
			printf("Number is zero.");
		}}
	else
	{
		printf("Number is negative.");
	}
	}
	
