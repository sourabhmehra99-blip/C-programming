write a program to count a number of dight .

/*#include<stdio.h>
void main()
{
	int n;
    int	count = 0;	
	printf("Enter the number: ");
	scanf("%d",&n);
	while(n>0)
	{
		count++;
		n = n/10;
		}	
		printf("The total number of dight %d",count);
}
