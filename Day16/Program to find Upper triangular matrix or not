#include<stdio.h>
void main()
{
	int r,c,j,i,flag=1;
	printf("Enter the size of r and c: ");
	scanf("%d%d",&r,&c);
	int a[r][c],b[r][c],mul[r][c];
	printf("Enter the elements of 2-D array\n");
	
	for(i=0;i<r;i++)
	{
		for(j=0;j<c;j++)
		{
			scanf("%d",&a[i][j]);
		}
	}
	printf("\nDisplay the elements of 2-D array A \n");
	for(i=0;i<r;i++)
	{
		for(j=0;j<c;j++)
		{
			printf("%d\t",a[i][j]);
		}
		printf("\n");

	}
    for(i=0;i<r;i++)
    {
    	for(j=0;j<i;j++)
    	{
    		if(a[i][j]!=0)
    		{
    			flag = 0;
    			break;
			}
		}
	}
	if (flag == 1)
	   printf("Upper triangular matrix");
	else
	  printf("not a uppper triangular matrix");
}
