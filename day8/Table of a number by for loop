Print the multiplication table of a given number.
#include<stdio.h>
void main()
{
	int num,i,d;
	printf("Enter the number for table: ");
	scanf("%d",&num);
	for(i=1;i<=10;i++)
	{
		d = num*i;
		printf("%d\n",d);
	}
}
