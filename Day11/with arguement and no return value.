//with arguement and no return value.
#include<stdio.h>
void add(float,float);
void add(float p,float q)// formal parameters 
{float sum;
	sum = p+q ;
	printf("Sum = %.2f\n",sum);
}
void main()
{
	float a,b,sum;
	printf("Enter the value of a and b: ");
	scanf("%f%f",&a,&b);
    add(a,b);// a and b are called actual perameters in function calling time.
}
