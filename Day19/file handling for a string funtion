#include<stdio.h>
#include<stdlib.h>
void main()
{
	FILE *fp;
	fp=fopen("abc.txt","r");
	if(fp == NULL)
	{
		printf("file not exist: ");
		exit(0);
	}
	char ch[100];
	fgets(ch,100,fp);
	printf("char = %s\n",ch);
	printf("Data read succesfully");
	fclose(fp);
}file 
