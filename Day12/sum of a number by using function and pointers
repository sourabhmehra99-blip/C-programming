// sum of numbers from 1 to N

#include<stdio.h>
void sum();
void sum(int n ,int *s)
{
	int i;
	*s = 0;
	for (i=1;i<=n;i++)
	{
		*s = *s+i;
	}
}
void main()
{
	int num,result;
	printf("Enter the Number: ");
	scanf("%d",&num);
	sum(num, &result);
	printf("sum=%d",result);
	return 0;
}
