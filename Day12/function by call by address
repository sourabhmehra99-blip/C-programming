//  function by call by address
#include<stdio.h>
void add(int*,int*);
void add(int *a,int *b)

{
	int sum;
	sum = *a+*b;
	printf("the number is %d",sum);
}
void main()
{
	int a,b;
	printf("Enter the a and b: ");
	scanf("%d%d",&a,&b);
	add(&a,&b);
}
