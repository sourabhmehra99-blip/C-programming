#include<stdio.h>

int main()
{
    int n,i,j;

    printf("Enter the element of string: ");
    scanf("%d",&n);

    getchar();

    char str[n];
    char arr[n];
    printf("Enter the first string: ");
    gets(str);
    
    printf("Enter the second  string: ");
    gets(arr);

    printf("The original first  string: ");
    puts(str);
    
    printf("The original second string: ");
    puts(arr);


    int len=0,bee=0;

    while(str[len]!='\0')
    {
        len++;
    }
     while(str[bee]!='\0')
    {
        bee++;
    }


    char temp;

    for(i=0;i<len;i++)
    {
        for(j=i+1;j<len;j++)
        {
            if(str[i]>str[j])
            {
                temp=str[i];
                str[i]=str[j];
                str[j]=temp;
            }
        }
    }
    for(i=0;i<bee;i++)
    {
    	for(j=i+1;j<bee;j++)
    	{
    		if(arr[i]>arr[j])
    		{
    			temp=arr[i];
    			arr[i]=arr[j];
    			arr[j]=temp;
			}
		}
	}
	int flag =1;
	if(len == bee){
	
	
	 for(i=0;i<len;i++)
	 {
	 	if(str[i]!=arr[i])
	 	{
	 		flag=0;
		 }
	 }}
	 else {
	 	printf("the string have different no of elements");
	 }
//    printf("Sorted first string: ");
//    puts(str);
//    printf("Sorted second string: ");
//    puts(arr);
    if(flag ==1)
    {
    	printf("the string is aragram .\n");
	}
	else
	{
		printf("The string is not aragram.");
	}

    return 0;
}
